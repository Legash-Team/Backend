PORT=3000

# MongoDB - local for now 
MONGO_URI=mongodb://localhost:27017/legash

# JWT
JWT_SECRET=
JWT_EXPIRES_IN=

# Email (Nodemailer + Gmail SMTP) - used for hospital/admin verification + password reset
EMAIL_USER=
EMAIL_APP_PASSWORD=
EMAIL_FROM=

# SMS OTP - groupmate's gateway, fill in once his API is confirmed
SMS_GATEWAY_BASE_URL=
SMS_GATEWAY_API_KEY=

# Super Admin seed values (used once by seed:superadmin script only)
SUPERADMIN_NAME=
SUPERADMIN_EMAIL=
SUPERADMIN_PASSWORD=

# Sprint 2 — push notifications (Firebase Cloud Messaging). Can stay empty in development.
FIREBASE_SERVICE_ACCOUNT_JSON=

# Cloudinary configuration for media uploads
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Frontend URL for email links
FRONTEND_URL="http://localhost:5173"
