// Donor auth routes — not built yet (Sprint 1 donor feature pending).
// Exporting an empty Router so mounting it in tests/apps doesn't crash.
// Dev A: fill in per legash_docs/AGENTS-Backend.md when implementing.
const express = require('express');
const router = express.Router();

module.exports = router;
